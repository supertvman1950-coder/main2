import random

elemants ='xzcvbnm'
pass_jength = int(input("Enter the desired password length: "))

password = ''.join(random.choice(elemants) for _ in range(int(pass_jength)))
print("Generated password:", password)
